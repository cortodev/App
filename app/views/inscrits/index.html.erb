<h1>Listing inscrits</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Email</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @inscrits.each do |inscrit| %>
  <tr>
    <td><%= inscrit.name %></td>
    <td><%= inscrit.email %></td>
    <td><%= link_to 'Show', inscrit %></td>
    <td><%= link_to 'Edit', edit_inscrit_path(inscrit) %></td>
    <td><%= link_to 'Destroy', inscrit, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Inscrit', new_inscrit_path %>
